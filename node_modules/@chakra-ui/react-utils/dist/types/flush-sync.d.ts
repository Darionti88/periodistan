import { AnyFunction } from "@chakra-ui/utils";
/** This is a workaround for React Concurrent Mode issue https://github.com/facebook/react/issues/18591. Remove once it's fixed. */
export declare function withFlushSync<T extends AnyFunction>(fn: any): (event: Parameters<T>[0]) => void;
//# sourceMappingURL=flush-sync.d.ts.map